; Notes:
; priority=1 --> Lower priorities indicate programs that start first and shut down last
; killasgroup=true --> send kill signal to child processes too

; graceful timeout should always be lower than stopwaitsecs to avoid orphan gunicorn workers.
[program:{{ cli_name }}-ragapp-web]
command={{ cli_dir }}/env/bin/gunicorn -b 127.0.0.1:{{ webserver_port }} -w {{ gunicorn_workers }} --max-requests {{ gunicorn_max_requests }} --max-requests-jitter {{ gunicorn_max_requests_jitter }} -t {{ http_timeout }} --graceful-timeout 30 ragapp.app:application --preload
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/web.log
stderr_logfile={{ cli_dir }}/logs/web.error.log
stopwaitsecs=40
killasgroup=true
user={{ user }}
directory={{ sites_dir }}
startretries={{ supervisor_startretries }}

[program:{{ cli_name }}-ragapp-schedule]
command={{ cli_cmd }} schedule
priority=3
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/schedule.log
stderr_logfile={{ cli_dir }}/logs/schedule.error.log
user={{ user }}
directory={{ cli_dir }}
startretries={{ supervisor_startretries }}

{% if not multi_queue_consumption %}
[program:{{ cli_name }}-ragapp-default-worker]
command={{ cli_cmd }} worker --queue default
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/worker.log
stderr_logfile={{ cli_dir }}/logs/worker.error.log
user={{ user }}
stopwaitsecs=1560
directory={{ cli_dir }}
killasgroup=true
numprocs={{ background_workers }}
process_name=%(program_name)s-%(process_num)d
startretries={{ supervisor_startretries }}
{% endif %}

[program:{{ cli_name }}-ragapp-short-worker]
command={{ cli_cmd }} worker --queue short{{',default' if multi_queue_consumption else ''}}
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/worker.log
stderr_logfile={{ cli_dir }}/logs/worker.error.log
user={{ user }}
stopwaitsecs=360
directory={{ cli_dir }}
killasgroup=true
numprocs={{ background_workers }}
process_name=%(program_name)s-%(process_num)d
startretries={{ supervisor_startretries }}

[program:{{ cli_name }}-ragapp-long-worker]
command={{ cli_cmd }} worker --queue long{{',default,short' if multi_queue_consumption else ''}}
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/worker.log
stderr_logfile={{ cli_dir }}/logs/worker.error.log
user={{ user }}
stopwaitsecs=1560
directory={{ cli_dir }}
killasgroup=true
numprocs={{ background_workers }}
process_name=%(program_name)s-%(process_num)d
startretries={{ supervisor_startretries }}

{% for worker_name, worker_details in workers.items() %}
[program:{{ cli_name }}-ragapp-{{ worker_name }}-worker]
command={{ cli_cmd }} worker --queue {{ worker_name }}
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/worker.log
stderr_logfile={{ cli_dir }}/logs/worker.error.log
user={{ user }}
stopwaitsecs={{ worker_details["timeout"] }}
directory={{ cli_dir }}
killasgroup=true
numprocs={{ worker_details["background_workers"] or background_workers }}
process_name=%(program_name)s-%(process_num)d
startretries={{ supervisor_startretries }}
{% endfor %}


{% if not skip_redis %}
[program:{{ cli_name }}-redis-cache]
command={{ redis_server }} {{ redis_cache_config }}
priority=1
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/redis-cache.log
stderr_logfile={{ cli_dir }}/logs/redis-cache.error.log
user={{ user }}
directory={{ sites_dir }}
startretries={{ supervisor_startretries }}

[program:{{ cli_name }}-redis-queue]
command={{ redis_server }} {{ redis_queue_config }}
priority=1
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/redis-queue.log
stderr_logfile={{ cli_dir }}/logs/redis-queue.error.log
user={{ user }}
directory={{ sites_dir }}
startretries={{ supervisor_startretries }}
{% endif %}

{% if node %}
[program:{{ cli_name }}-node-socketio]
command={{ node }} {{ cli_dir }}/apps/ragapp/socketio.js
priority=4
autostart=true
autorestart=true
stdout_logfile={{ cli_dir }}/logs/node-socketio.log
stderr_logfile={{ cli_dir }}/logs/node-socketio.error.log
user={{ user }}
directory={{ cli_dir }}
startretries={{ supervisor_startretries }}
{% endif %}

[group:{{ cli_name }}-web]
programs={{ cli_name }}-ragapp-web {%- if node -%} ,{{ cli_name }}-node-socketio {%- endif%}


{% if multi_queue_consumption %}

[group:{{ cli_name }}-workers]
programs={{ cli_name }}-ragapp-schedule,{{ cli_name }}-ragapp-short-worker,{{ cli_name }}-ragapp-long-worker{%- for worker_name in workers -%},{{ cli_name }}-ragapp-{{ worker_name }}-worker{%- endfor %}

{% else %}

[group:{{ cli_name }}-workers]
programs={{ cli_name }}-ragapp-schedule,{{ cli_name }}-ragapp-default-worker,{{ cli_name }}-ragapp-short-worker,{{ cli_name }}-ragapp-long-worker{%- for worker_name in workers -%},{{ cli_name }}-ragapp-{{ worker_name }}-worker{%- endfor %}

{% endif %}

{% if not skip_redis %}
[group:{{ cli_name }}-redis]
programs={{ cli_name }}-redis-cache,{{ cli_name }}-redis-queue
{% endif %}
